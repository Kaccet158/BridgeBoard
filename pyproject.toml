[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bridgeboard"
version = "0.1.0"
dependencies = [
    "pyperclip",
    "webdavclient3",
    "plyer"
]

[project.scripts]
bb = "bridgeboard.main:main"